# 1D-Arrays
A 1D Array is a data structure that stores multiple values of the same data type in a single variable. Each element is stored in a sequential order and can be accessed using its index, which starts from 0. It makes data management easier and reduces the need to create multiple variables.  
